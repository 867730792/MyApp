<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="19" />
  <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
   
 <activity
            android:name="activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name="activtity.timeBroad">
            <intent-filter>
	<action android:name="android.intent.action.BOOT_COMPLETED" />
	</intent-filter>
        </receiver>
         <activity
            android:name="android.support.v4.view.Viewpagertest">
            </activity> 
               
        <activity
            android:name="activity.sportActivity">
            <intent-filter >
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity
            android:name="activity.Mynote">
            </activity>
            <activity
            android:name="activity.time">
            </activity>
            <activity
            android:name="activity.Search">
            </activity>
        <activity
            android:name="colorie.CalorieActivity">
            </activity>    
        <activity
            android:name="activity.BMI">
            </activity>        
           <activity
            android:name="activity.FoodActivity">
            </activity> 
            <activity
            android:name="activity.ActionCenter">
            </activity>     
    </application>
</manifest>
